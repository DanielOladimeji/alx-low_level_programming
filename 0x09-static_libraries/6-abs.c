#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @r: the int to check
 * Return: the absolute value of the int checked
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	return (-r);
}
